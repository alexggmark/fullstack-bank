const express = require('express')
const router = express.Router()
const controllerCurrent = require('../controllers/translog.controller')
// const auth = require('../middleware/auth')

router.get(
  '/getLogUser',
  // TODO:
)

router.post(
  '/createNewLog',
  // TODO:
)

module.exports = router